{{! H3 — 로그인 전용 헤더. L3 레이아웃 (화면 05) }}
<header class="bg-white border-b border-slate-200 h-16 flex items-center justify-center shrink-0">
  <h1 class="text-xl font-bold text-slate-800">MediCare+ 내부 시스템</h1>
</header>
