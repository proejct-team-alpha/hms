# W3-2 증상 입력 JS fetch 호출 골격 구현

## 작업 목표
`setTimeout` 방식을 `async/await` + Promise 기반 fetch 골격으로 교체.
W4에서 실제 `POST /llm/symptom/analyze` 연동 시 `callSymptomApi()` 함수만 수정하면 되는 구조.

## 작업 목록
<!-- DONE 1. callSymptomApi(text) async 함수 정의 (더미 Promise 반환, W4 실제 fetch로 교체 예정) -->
<!-- DONE 2. analyzeBtn click 핸들러를 async/await 구조로 변경 -->
<!-- DONE 3. try/catch 오류 처리 골격 추가 (W3-5 폴백 UI 연결 예정) -->

## 수정 파일
- `src/main/resources/templates/reservation/symptom-reservation.mustache` (script 블록)
