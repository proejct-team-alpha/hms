# W2-5.1 Workflow — 예약번호 발급 및 예약 조회 화면

> **작성일**: 2W
> **목표**: `RES-YYYYMMDD-XXX` 형식 예약번호 발급 + 예약 조회 화면 구현

---

## 전체 흐름

```
예약 생성 시 RES-YYYYMMDD-XXX(당월 누적) 형식 발급
  → 완료 화면에 예약번호 표시
예약번호 또는 이름+전화번호로 조회 가능
```

---

## 인터뷰 결과

| 항목 | 내용 |
|------|------|
| 예약번호 형식 | `RES-YYYYMMDD-XXX` (당월 누적 카운트) |
| 조회 방법 | 예약번호 단건 조회 OR 이름+전화번호 목록 조회 |
| 완료 화면 | 예약번호 + 조회 버튼 추가 |
| 메인 화면 | 예약 조회 카드 추가 |

---

## 실행 흐름

```
[1] ReservationRepository — countByCreatedAtBetween() 추가
[2] ReservationInfoDto — 조회 결과 DTO 신규
[3] ReservationCompleteInfo — reservationNumber 필드 추가
[4] ReservationService — generateReservationNumber() + 조회 메서드 수정
[5] ReservationController — GET /reservation/lookup 추가
[6] reservation-lookup.mustache — 조회 화면 신규
[7] reservation-complete.mustache — 예약번호 표시 + 조회 버튼
[8] patient-choice.mustache — 예약 조회 카드 추가
```

---

## UI Mockup

```
[예약 조회 화면]
┌─────────────────────────────────────────────┐
│  [예약번호로 조회] [이름+전화번호로 조회]     │  탭 전환
├─────────────────────────────────────────────┤
│ 예약번호 [RES-20260310-001] [조회]           │
├─────────────────────────────────────────────┤
│ 예약번호: RES-20260310-001                   │
│ 환자명:   홍길동                             │
│ 진료과:   내과 / 전문의: 의사이영희          │
│ 일시:     2026-03-20 09:00                   │
│ 상태:     CONFIRMED                          │
└─────────────────────────────────────────────┘

[완료 화면 변경]
예약번호: RES-20260320-001  ← 추가
[메인으로 가기] [예약 조회]  ← 조회 버튼 추가
```

---

## 작업 목록

1. `ReservationRepository.java` — `countByCreatedAtBetween()` 추가
2. `ReservationInfoDto.java` — 조회 결과 표시용 DTO 신규 생성
3. `ReservationCompleteInfo.java` — `reservationNumber` 필드 추가
4. `ReservationService.java` — `generateReservationNumber()` + 조회 메서드 DTO 반환으로 수정
5. `ReservationController.java` — `GET /reservation/lookup` 추가
6. `reservation-lookup.mustache` — 조회 화면 신규 생성
7. `reservation-complete.mustache` — 예약번호 표시 + 조회 버튼 추가
8. `patient-choice.mustache` — 예약 조회 카드 추가

---

## 작업 진행내용

- [x] ReservationRepository countByCreatedAtBetween() 추가
- [x] ReservationInfoDto 신규 생성
- [x] ReservationCompleteInfo reservationNumber 추가
- [x] ReservationService 예약번호 생성 + 조회 메서드 수정
- [x] ReservationController GET /reservation/lookup 추가
- [x] reservation-lookup.mustache 신규 생성
- [x] reservation-complete.mustache 수정
- [x] patient-choice.mustache 수정

---

## 실행 흐름에 대한 코드

### ReservationService — generateReservationNumber()

```java
private String generateReservationNumber() {
    LocalDateTime now = LocalDateTime.now();
    LocalDateTime start = now.withDayOfMonth(1).withHour(0).withMinute(0).withSecond(0).withNano(0);
    LocalDateTime end = start.plusMonths(1).minusNanos(1);
    long count = reservationRepository.countByCreatedAtBetween(start, end) + 1;
    String dateStr = now.format(DateTimeFormatter.ofPattern("yyyyMMdd"));
    return String.format("RES-%s-%03d", dateStr, count);
}
```

### ReservationController — GET /reservation/lookup

```java
@GetMapping("/lookup")
public String lookupPage(HttpServletRequest request) {
    String reservationNumber = request.getParameter("reservationNumber");
    String name  = request.getParameter("name");
    String phone = request.getParameter("phone");

    if (reservationNumber != null && !reservationNumber.isBlank()) {
        reservationService.findByReservationNumber(reservationNumber)
            .ifPresentOrElse(
                dto -> request.setAttribute("reservation", dto),
                () -> request.setAttribute("errorMessage", "예약을 찾을 수 없습니다."));
    } else if (name != null && !name.isBlank() && phone != null && !phone.isBlank()) {
        List<ReservationInfoDto> list = reservationService.findByPhoneAndName(phone, name);
        if (list.isEmpty()) request.setAttribute("errorMessage", "예약을 찾을 수 없습니다.");
        else request.setAttribute("reservations", list);
    }
    request.setAttribute("pageTitle", "예약 조회");
    return "reservation/reservation-lookup";
}
```

---

## 테스트 진행

| 케이스 | 조건 | 기대 결과 |
|--------|------|-----------|
| 예약번호 조회 | 유효한 번호 | 단건 예약 정보 표시 |
| 이름+전화 조회 | 일치하는 데이터 | 목록 표시 |
| 존재하지 않는 번호 | 조회 | 오류 메시지 표시 |
| 완료 화면 | 예약 생성 후 | 예약번호 표시 확인 |

---

## 완료 기준

- [x] `RES-YYYYMMDD-XXX` 형식 예약번호 생성
- [x] 예약번호 단건 조회 동작
- [x] 이름+전화번호 목록 조회 동작
- [x] 완료 화면에 예약번호 표시
- [x] patient-choice에 예약 조회 카드 추가
