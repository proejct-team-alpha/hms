# 작업 보고서 - Task 30-7 테스트 보강

- **작업 일시**: 2026-03-21 21:12
- **진행 상태**: 완료

## 1. 전체 작업 흐름 (Workflow)

1. 먼저 [task-030.md](/c:/Users/HSystem/hms/doc/dev-c/task/task-030.md)와 [workflow-030.md](/c:/Users/HSystem/hms/doc/dev-c/workflow/workflow-030.md)를 다시 확인해 이번 단계 목표를 "등록/수정 검증 테스트 범위를 문서 계약에 맞게 닫는 것"으로 고정했다.
2. 그다음 [AdminRuleControllerTest.java](/c:/Users/HSystem/hms/src/test/java/com/smartclinic/hms/admin/rule/AdminRuleControllerTest.java)를 다시 점검해, 빈 값 실패와 category 누락 실패는 이미 등록/수정 모두 존재하지만 `content` 길이 초과는 명시 테스트가 없다는 점을 확인했다.
3. 그래서 이번 단계에서는 컨트롤러 테스트에 `content` 3000자 초과 케이스를 등록과 수정 각각 한 개씩 추가했다.
4. 등록 쪽 테스트는 `admin/rule-new` same-view 재렌더링, `contentError` 노출, 선택한 카테고리 유지, 제목 유지, textarea 입력 유지까지 함께 확인하도록 구성했다.
5. 수정 쪽 테스트도 `admin/rule-detail` same-view 재렌더링, `rule`/`model` 공존, `contentError` 노출, 선택한 카테고리 유지, 수정 중 입력 유지까지 함께 확인하도록 맞췄다.
6. 마지막으로 [AdminRuleControllerTest.java](/c:/Users/HSystem/hms/src/test/java/com/smartclinic/hms/admin/rule/AdminRuleControllerTest.java)와 [AdminRuleServiceTest.java](/c:/Users/HSystem/hms/src/test/java/com/smartclinic/hms/admin/rule/AdminRuleServiceTest.java)를 함께 실행해 회귀가 없는지 확인하고 이번 단계를 마감했다.

## 2. 핵심 코드

```java
@Test
@DisplayName("new rule content too long rerenders form and preserves inputs")
void createRule_withContentTooLong_rerendersFormAndPreservesInputs() throws Exception {
    String longContent = "content-".repeat(376);

    mockMvc.perform(post("/admin/rule/new")
                    .param("title", "supply-rule")
                    .param("content", longContent)
                    .param("category", "SUPPLY")
                    .param("active", "true"))
            .andExpect(view().name("admin/rule-new"))
            .andExpect(request().attribute("contentError", "내용은 3000자 이하로 입력해 주세요."));
}
```

```java
@Test
@DisplayName("update content too long rerenders detail view and preserves edited inputs")
void updateRule_withContentTooLong_rerendersDetailViewAndPreservesEditedInputs() throws Exception {
    String longContent = "content-".repeat(376);

    mockMvc.perform(post("/admin/rule/update")
                    .param("ruleId", "5")
                    .param("title", "supply-rule")
                    .param("content", longContent)
                    .param("category", "SUPPLY")
                    .param("active", "true"))
            .andExpect(view().name("admin/rule-detail"))
            .andExpect(request().attribute("contentError", "내용은 3000자 이하로 입력해 주세요."));
}
```

## 3. 쉬운 비유

- 이번 단계는 이미 붙어 있던 문 손잡이를 새로 만드는 작업이 아니라, 아직 확인하지 않았던 잠금 장치 한 개를 추가로 시험해 보는 작업에 가까웠다.
- 제목 길이 초과는 이미 확인했지만 내용 길이 초과는 빠져 있었기 때문에, 그 빈 칸만 채워서 등록/수정 두 문이 같은 기준으로 잠기는지 확인한 셈이다.

## 4. 기술 메모

- 빈 값 실패와 category 누락 실패는 이미 컨트롤러 테스트에 있었기 때문에, 이번 단계의 실질적인 추가 작업은 `content` 길이 초과 회귀 테스트 보강이었다.
- 서비스 레이어는 DTO 검증 전에 호출되지 않기 때문에, 이번 범위에서 `AdminRuleServiceTest`에 새 검증 케이스를 추가하는 것보다 기존 테스트를 재실행해 회귀가 없는지 확인하는 편이 더 정확했다.
- 새 테스트는 모두 same-view 렌더링과 입력 유지에 초점을 맞췄고, 단순히 오류 메시지만 보는 것이 아니라 category selected 상태와 폼 값 유지까지 같이 확인했다.

## 5. 검증 결과

- 실행한 테스트
  - `.\gradlew.bat test --no-watch-fs --tests "com.smartclinic.hms.admin.rule.AdminRuleControllerTest" --tests "com.smartclinic.hms.admin.rule.AdminRuleServiceTest"`
- 결과
  - `BUILD SUCCESSFUL`

## 6. 변경 파일

- [AdminRuleControllerTest.java](/c:/Users/HSystem/hms/src/test/java/com/smartclinic/hms/admin/rule/AdminRuleControllerTest.java)
- [task-030.md](/c:/Users/HSystem/hms/doc/dev-c/task/task-030.md)
- [report-20260321-2112-task-30-7.md](/c:/Users/HSystem/hms/doc/dev-c/.person/reports/task-030/report-20260321-2112-task-30-7.md)

## 7. 다음 단계

- `Task 30-8`에서 `task-030` 체크리스트와 최종 검증 결과를 실제 구현 기준으로 마감하면 된다.
- 이때 `Task 30-1`부터 `Task 30-7`까지 누적된 리포트와 테스트 결과를 기준으로 완료 기준 충족 여부를 한 번에 정리하면 자연스럽다.

## 8. 남은 TODO / 리스크

- 이번 단계는 컨트롤러 중심 검증 보강이라 Mustache 전용 렌더링 테스트를 별도로 추가하지는 않았다.
- 메시지 계약은 지금 DTO와 컨트롤러 테스트 기준으로 고정됐지만, 이후 문구가 다시 바뀌면 등록/수정 양쪽 테스트 기대값을 함께 수정해야 한다.
