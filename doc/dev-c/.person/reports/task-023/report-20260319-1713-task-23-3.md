# 이번 작업 보고서: 환자 수정 서비스 로직 구현 (Task 23-3)

- **작업 일시**: 2026-03-19 17:13 (Asia/Seoul)
- **진행 상태**: 완료

## 1. 전체 작업 흐름 (Workflow)

1. [workflow-023.md](c:/workspace/Team/hms/doc/dev-c/workflow/workflow-023.md)와 [task-023.md](c:/workspace/Team/hms/doc/dev-c/task/task-023.md)를 기준으로, 이번 단계의 목표를 “환자 수정 API를 붙이기 전에 서비스 규칙을 먼저 안정화하는 것”으로 잡았다. 특히 이번 기능은 이름/연락처/메모만 수정해야 하고, 전화번호 중복은 반드시 막아야 하므로, API보다 서비스와 서비스 테스트를 먼저 닫는 쪽이 더 안전하다고 판단했다.
2. 먼저 [AdminPatientRepository.java](c:/workspace/Team/hms/src/main/java/com/smartclinic/hms/admin/patient/AdminPatientRepository.java)에 전화번호 중복 확인 메서드를 추가했다. 현재 시스템은 환자 동일성을 사실상 전화번호로 판단하므로, “다른 환자와 같은 전화번호로 수정하는 경우”를 정확히 막을 수 있는 쿼리가 필요했다. 여기서는 자기 자신은 제외하고, 하이픈/공백을 제거한 값 기준으로 비교하도록 `existsByNormalizedPhoneAndIdNot(...)`를 추가했다.
3. 다음으로 [AdminPatientService.java](c:/workspace/Team/hms/src/main/java/com/smartclinic/hms/admin/patient/AdminPatientService.java)를 정리했다. 새로 추가한 `updatePatient(...)` 메서드는 먼저 `patientId`로 대상을 찾고, 없으면 `CustomException.notFound(...)`를 던진다. 이후 이름과 연락처는 `trim()`으로 정리하고, 전화번호는 비교 전용 값으로 한 번 더 하이픈/공백을 제거한다. 이 값으로 다른 환자와의 중복을 확인한 뒤, 실제 저장은 기존 `Patient.updateInfo(...)`를 사용해 진행했다.
4. 여기서 중요한 포인트는 부분 수정 범위와 기존 엔티티 메서드의 간극을 서비스에서 흡수했다는 점이다. `Patient.updateInfo(...)`는 이메일과 주소까지 같이 받는 메서드이므로, 이번 API 범위에서는 기존 `email`, `address`를 그대로 유지한 채 `name`, `phone`, `note`만 바꾸도록 조합했다. `note`는 선택 입력이라, `trim()` 후 빈 문자열이면 `null`로 정규화하는 정책도 이 단계에서 함께 확정했다.
5. 마지막으로 [AdminPatientServiceTest.java](c:/workspace/Team/hms/src/test/java/com/smartclinic/hms/admin/patient/AdminPatientServiceTest.java)를 보강했다. 수정 성공, 없는 환자, 다른 환자와 전화번호 중복, 동일 전화번호 유지 허용, blank note를 null로 바꾸는 시나리오를 추가해서 서비스 규칙이 충분히 닫혔는지 확인했다. 테스트가 모두 통과한 뒤 [task-023.md](c:/workspace/Team/hms/doc/dev-c/task/task-023.md)에 Task 23-3 완료와 메모를 현재 상태 기준으로 다시 정리했다.

## 2. 핵심 코드/구조 (Core Logic)

```java
@Transactional
public UpdateAdminPatientApiResponse updatePatient(Long patientId, String name, String phone, String note) {
    Patient patient = adminPatientRepository.findById(patientId)
            .orElseThrow(() -> CustomException.notFound(PATIENT_NOT_FOUND_MESSAGE));

    String normalizedName = normalizeRequiredText(name);
    String normalizedPhone = normalizeRequiredText(phone);
    String comparablePhone = normalizeComparablePhone(normalizedPhone);
    String normalizedNote = normalizeOptionalText(note);

    if (adminPatientRepository.existsByNormalizedPhoneAndIdNot(patientId, comparablePhone)) {
        throw CustomException.conflict(DUPLICATE_PATIENT_PHONE_ERROR_CODE, DUPLICATE_PATIENT_PHONE_MESSAGE);
    }

    patient.updateInfo(
            normalizedName,
            normalizedPhone,
            patient.getEmail(),
            patient.getAddress(),
            normalizedNote
    );

    return new UpdateAdminPatientApiResponse(
            patient.getId(),
            patient.getName(),
            patient.getPhone(),
            patient.getNote(),
            PATIENT_UPDATED_MESSAGE
    );
}
```

```java
@Query("""
        SELECT COUNT(p) > 0
        FROM Patient p
        WHERE p.id <> :patientId
          AND REPLACE(REPLACE(p.phone, '-', ''), ' ', '') = :normalizedPhone
        """)
boolean existsByNormalizedPhoneAndIdNot(...)
```

핵심은 “부분 수정 API인데도 엔티티는 전체 수정 메서드만 가진 상황”을 서비스에서 안전하게 흡수한 것이다. 서비스가 기존 필드를 보존하고, 중복 전화번호를 먼저 막고, 메모 정규화까지 책임지게 하면서 다음 API 단계가 훨씬 단순해졌다.

## 3. 쉽게 이해하는 비유 (Easy Analogy)

- 이번 작업은 오래된 서류 양식에 새 체크 항목을 붙이는 것과 비슷하다.
- 서류 양식은 원래 이름, 연락처, 이메일, 주소, 메모를 다 한 번에 다시 쓰게 되어 있는데, 우리는 이번에 이름/연락처/메모만 바꾸고 싶다.
- 그래서 서비스가 “이메일과 주소는 예전 값 그대로 유지하고, 바꾸려는 칸만 새로 적어 제출하는 역할”을 맡게 된 셈이다.

## 4. 기술 딥다이브 (Technical Deep-dive)

- **부분 수정과 필드 보존**: 엔티티에 부분 수정 전용 메서드가 없을 때는 서비스가 기존 상태를 읽고 필요한 값만 바꿔 조합하는 방식이 안전하다. 이번 구현은 `email`, `address`를 그대로 보존하면서 API 범위를 넘지 않게 했다.
- **전화번호 중복 방지 기준**: 현재 시스템은 전화번호로 기존 환자를 재사용하므로, 수정 시 중복 전화번호를 허용하면 환자 식별 규칙이 무너질 수 있다. 그래서 자기 자신을 제외한 중복 여부를 하이픈/공백 제거 기준으로 검사하도록 리포지토리 쿼리를 추가했다.
- **선택 입력 `note`의 정규화**: `note`는 입력 자체는 선택이지만, 저장 시 빈 문자열과 `null`이 섞이면 후속 화면이나 API에서 다루기 불편해진다. 이번에는 `trim()` 후 비어 있으면 `null`로 통일하는 쪽으로 정책을 확정했다.
- **테스트를 서비스 단계에서 먼저 닫은 이유**: API 컨트롤러를 먼저 붙이면 실패 원인이 검증/JSON 바인딩/서비스 규칙 중 어디서 났는지 분리하기 어려워진다. 그래서 서비스 테스트로 핵심 규칙을 먼저 닫아두면 다음 API 테스트 단계가 훨씬 단순해진다.

## 5. 검증 결과 (Verification)

- 실행 명령: `./gradlew test --tests 'com.smartclinic.hms.admin.patient.AdminPatientServiceTest'`
- 결과: `BUILD SUCCESSFUL`
- 확인 범위:
  - 환자 수정 성공
  - 없는 환자 예외
  - 다른 환자와 전화번호 중복 차단
  - 동일 전화번호 유지 허용
  - blank note → null 정규화

## 6. 변경 파일 (Changed Files)

- [AdminPatientRepository.java](c:/workspace/Team/hms/src/main/java/com/smartclinic/hms/admin/patient/AdminPatientRepository.java)
- [AdminPatientService.java](c:/workspace/Team/hms/src/main/java/com/smartclinic/hms/admin/patient/AdminPatientService.java)
- [AdminPatientServiceTest.java](c:/workspace/Team/hms/src/test/java/com/smartclinic/hms/admin/patient/AdminPatientServiceTest.java)
- [task-023.md](c:/workspace/Team/hms/doc/dev-c/task/task-023.md)

## 7. 다음 작업 메모 (Next Step)

- 다음 단계는 `Task 23-4`이다.
- 여기서 `AdminPatientApiController`를 추가하고 `POST /admin/api/patients/{id}/update`를 `@Valid @RequestBody` + `Resp.ok(...)` 패턴으로 연결하면 된다.
- 서비스 로직은 이미 닫혀 있으니, 이후 `Task 23-5`에서는 JSON 요청/응답, 검증 실패, 중복 전화번호, 없는 환자 시나리오를 API 테스트로 바로 보강하면 된다.