# task-019

## 목적
- [x] `workflow-019`의 직원·진료과 SSR 폼 유효성 검증 범위를 구현 가능한 작업 단위로 분해한다.
- [x] 관리자 입력 폼 전반에 `@Valid` 기반 기본 검증과 에러 표시 흐름을 적용한다.
- [x] 검증 실패 시 에러 메시지와 기존 입력값이 유지되는 SSR UX를 공통 패턴으로 정리한다.
- [x] API 확장보다 SSR 입력 검증 흐름 정리에 우선순위를 둔다.
- [x] 컨트롤러, 템플릿, 테스트를 함께 정리해 회귀를 방지한다.

## Task 19-1. 현재 폼 DTO 검증 구조 점검
- [x] 직원 등록/수정, 진료과 등록/수정에서 사용하는 입력 DTO와 컨트롤러 시그니처를 점검한다.
- [x] 각 폼의 필수 입력 필드와 현재 검증 누락 지점을 정리한다.
- [x] `BindingResult`를 붙여야 하는 컨트롤러와 뷰 매핑 흐름을 확인한다.
- [x] Mustache 템플릿에서 입력값 유지와 에러 메시지 출력이 필요한 위치를 메모한다.
- [x] 검증 실패 시 redirect 대신 same-view 재렌더링이 필요한 지점을 정리한다.

## Task 19-2. 직원 등록 폼 유효성 검증 적용
- [x] 직원 등록 입력 DTO에 이름, 부서, 로그인 아이디, 비밀번호 필수 검증을 정리한다.
- [x] 직원 등록 컨트롤러에 `@Valid`와 `BindingResult`를 적용한다.
- [x] 검증 실패 시 등록 폼을 same-view로 다시 렌더링한다.
- [x] 등록 폼 템플릿에 필드별 에러 메시지와 기존 입력값 유지 구조를 반영한다.
- [x] 직원 등록 검증 실패 시나리오를 컨트롤러 테스트로 추가한다.

## Task 19-3. 직원 수정 폼 유효성 검증 적용
- [x] 직원 수정 입력 DTO에 이름과 부서 필수 검증을 추가한다.
- [x] 수정 시 비밀번호는 선택 입력으로 유지한다.
- [x] 직원 수정 컨트롤러에 `@Valid`와 `BindingResult`를 적용한다.
- [x] 검증 실패 시 상세/수정 폼에서 기존 입력값과 에러 메시지가 유지되도록 정리한다.
- [x] 직원 수정 검증 실패 시나리오를 컨트롤러 테스트로 추가한다.

## Task 19-4. 진료과 목록 인라인 등록 검증 적용
- [x] 진료과 등록 입력 객체에 진료과명 필수 검증을 추가한다.
- [x] 목록 인라인 등록 컨트롤러에 `@Valid`와 `BindingResult`를 적용한다.
- [x] 검증 실패 시 목록 화면 same-view 재렌더링과 입력값 유지를 적용한다.
- [x] 목록 템플릿에 인라인 등록 에러 메시지와 모달 재오픈 구조를 반영한다.
- [x] 진료과 인라인 등록 검증 실패 시나리오를 컨트롤러 테스트로 추가한다.

## Task 19-5. 진료과 상세/수정 검증 적용
- [x] 진료과 수정 입력 객체에 `departmentId`, `name` 필수 검증을 추가한다.
- [x] 상세/수정 컨트롤러에 `@Valid`와 `BindingResult`를 적용한다.
- [x] DTO 검증 실패 시 상세 화면 same-view 재렌더링과 `editName` 유지를 적용한다.
- [x] 상세 템플릿에 필드별 에러 메시지 영역을 추가한다.
- [x] 진료과 수정 검증 실패 시나리오를 컨트롤러 테스트로 추가한다.

## Task 19-6. SSR 검증 UX 공통 패턴 및 테스트 보강
- [x] 직원/진료과 폼의 에러 메시지 주입 방식을 공통 유틸로 정리한다.
- [x] 빈 문자열과 공백 입력이 모두 검증 실패로 처리되는지 테스트로 확인한다.
- [x] DTO 검증 실패 시 기존 서비스 검증이 실행되지 않는 흐름을 테스트로 보강한다.
- [x] `admin.staff`, `admin.department` 범위 테스트를 실행해 누락 여부를 확인한다.
- [x] 현재 작업 범위 기준으로 별도 known issue 없이 마무리 가능한 상태인지 확인한다.

## 완료 기준
- [x] 직원 등록 폼에서 필수값 누락 시 에러 메시지가 표시된다.
- [x] 직원 수정 폼에서 필수값 누락 시 에러 메시지가 표시된다.
- [x] 진료과 목록 인라인 등록에서 진료과명 누락 시 에러 메시지가 표시된다.
- [x] 진료과 상세/수정 폼에서 진료과명 누락 시 에러 메시지가 표시된다.
- [x] 검증 실패 시 기존 입력값이 유지된다.
- [x] 관련 SSR 컨트롤러와 범위 테스트가 통과한다.

## 추천 진행 순서
- [x] Task 19-1 현재 구조 점검
- [x] Task 19-2 직원 등록 검증 적용
- [x] Task 19-3 직원 수정 검증 적용
- [x] Task 19-4 진료과 목록 인라인 등록 검증 적용
- [x] Task 19-5 진료과 상세/수정 검증 적용
- [x] Task 19-6 공통 패턴 정리 및 테스트 보강

## 메모
- [x] 직원 등록/수정은 기존 `@Valid + BindingResult` 골격을 활용하고, 필수값 보강과 same-view 재렌더링 패턴을 정리했다.
- [x] `staff-form.mustache`는 등록/수정 화면을 함께 쓰므로 `usernameError`, `passwordError`, `nameError`, `departmentIdError` 같은 필드 에러를 공통 패턴으로 노출하도록 맞췄다.
- [x] 진료과 등록은 `CreateAdminDepartmentRequest`, 수정은 `UpdateAdminDepartmentRequest`로 분리해 `@Valid`를 자연스럽게 적용했다.
- [x] 진료과 등록 검증 실패 시 `createName`, `createActive`, `openCreateModal`, `nameError`를 유지해 목록 화면 same-view에서 모달을 다시 열도록 정리했다.
- [x] 진료과 수정 검증 실패 시 상세 화면 same-view에서 `editName`과 `nameError`를 유지하고, 서비스 검증 실패는 기존 flash redirect 흐름을 유지했다.
- [x] `SsrValidationViewSupport`를 추가해 직원/진료과 컨트롤러가 같은 안내 문구와 같은 필드 에러 주입 방식을 사용하도록 정리했다.
- [x] 직원 등록의 공백 로그인 아이디, 직원 수정의 빈 이름, 진료과 등록의 빈 문자열/공백 이름, 진료과 수정의 공백 이름을 테스트로 보강했다.
- [x] DTO 검증 실패 시 서비스 생성/수정 메서드가 호출되지 않는지 컨트롤러 테스트로 확인했다.
- [x] 범위 검증은 `./gradlew test --tests 'com.smartclinic.hms.admin.staff.*' --tests 'com.smartclinic.hms.admin.department.*'`로 확인했고 `BUILD SUCCESSFUL` 상태다.
