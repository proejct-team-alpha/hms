# task-020

## 목적
- [x] `workflow-020`의 관리자 서비스 단위 테스트 보강 범위를 구현 가능한 작업 단위로 분해한다.
- [x] `AdminStaffServiceTest`, `AdminDepartmentServiceTest`를 중심으로 핵심 CRUD 시나리오를 보강한다.
- [x] 중복 방지, 비밀번호 암호화, 상태 전이 같은 핵심 비즈니스 규칙을 테스트로 고정한다.
- [x] 기존 테스트 자산은 유지하되, 읽기 쉬운 구조로 정리한다.
- [x] `admin.staff`, `admin.department` 범위 회귀를 테스트로 방지한다.

## Task 20-1. 현재 서비스 테스트 구조 점검
- [x] `AdminStaffServiceTest`의 현재 커버리지와 누락 시나리오를 점검한다.
- [x] `AdminDepartmentServiceTest`의 현재 커버리지와 누락 시나리오를 점검한다.
- [x] 중복 방지, 암호화, 상태 변경 관련 테스트가 이미 있는지 여부를 정리한다.
- [x] 테스트 이름과 배치가 읽기 어려운 지점을 메모한다.
- [x] 이후 단계에서 우선 보강할 시나리오 순서를 정한다.

## Task 20-2. 직원 생성/중복 방지/암호화 테스트 보강
- [ ] 직원 생성 성공 시나리오를 정리한다.
- [ ] 비밀번호가 BCrypt로 암호화되어 저장되는지 확인한다.
- [ ] `DOCTOR` 생성 시 `Doctor` 엔티티가 함께 생성되는지 확인한다.
- [ ] `username` 중복 방지 시나리오를 보강한다.
- [ ] `employeeNumber` 중복 방지 시나리오를 보강한다.

## Task 20-3. 직원 수정/비활성화 테스트 보강
- [ ] 이름과 부서 수정 시나리오를 정리한다.
- [ ] 비밀번호 입력 시 변경되는지 확인한다.
- [ ] 비밀번호 미입력 시 기존 비밀번호가 유지되는지 확인한다.
- [ ] `DOCTOR` 수정 시 전문 분야와 진료 가능 요일 반영을 확인한다.
- [ ] 비활성화 성공, 본인 계정 비활성화 차단, 이미 비활성화된 직원 재요청 차단을 보강한다.

## Task 20-4. 진료과 생성/수정/중복 방지 테스트 보강
- [ ] 진료과 생성 성공 시나리오를 정리한다.
- [ ] `active` 값이 요청대로 저장되는지 확인한다.
- [ ] 진료과명 수정 성공 시나리오를 정리한다.
- [ ] 공백 이름 차단 시나리오를 보강한다.
- [ ] 중복 이름 차단과 없는 ID 차단 시나리오를 보강한다.

## Task 20-5. 진료과 상태 전이 테스트 보강
- [ ] 비활성화 성공 시나리오를 정리한다.
- [ ] 이미 비활성화된 진료과 재요청 차단을 보강한다.
- [ ] 활성화 성공 시나리오를 정리한다.
- [ ] 이미 활성화된 진료과 재요청 차단을 보강한다.
- [ ] 없는 진료과 상태 변경 요청 차단을 보강한다.

## Task 20-6. 테스트 구조 정리 및 범위 검증
- [ ] 두 서비스 테스트 파일의 Given-When-Then 흐름을 읽기 쉽게 정리한다.
- [ ] 중복되거나 의미가 겹치는 테스트 이름과 배치를 정리한다.
- [ ] `admin.staff`, `admin.department` 범위 테스트를 실행해 회귀 여부를 확인한다.
- [ ] 필요 시 전체 테스트 실행 전 known issue 여부를 다시 정리한다.
- [ ] `task-020` 완료 기준과 메모를 최종 상태로 갱신한다.

## 완료 기준
- [ ] `AdminStaffServiceTest`에 생성/수정/중복 방지/암호화/비활성화 핵심 시나리오가 보강된다.
- [ ] `AdminDepartmentServiceTest`에 생성/수정/중복 방지/상태 전이 핵심 시나리오가 보강된다.
- [ ] 두 테스트 파일이 기존보다 읽기 쉬운 구조로 정리된다.
- [ ] `admin.staff`, `admin.department` 범위 테스트가 통과한다.

## 추천 진행 순서
- [x] Task 20-1 현재 서비스 테스트 구조 점검
- [ ] Task 20-2 직원 생성/중복 방지/암호화 테스트 보강
- [ ] Task 20-3 직원 수정/비활성화 테스트 보강
- [ ] Task 20-4 진료과 생성/수정/중복 방지 테스트 보강
- [ ] Task 20-5 진료과 상태 전이 테스트 보강
- [ ] Task 20-6 테스트 구조 정리 및 범위 검증

## 메모
- [x] 이번 작업은 컨트롤러가 아니라 서비스 테스트 중심으로 진행한다.
- [x] 비밀번호 암호화 검증은 단순 문자열 비교가 아니라 `PasswordEncoder.matches(...)` 기준으로 확인한다.
- [x] 중복 방지 테스트는 예외 타입뿐 아니라 메시지와 영향 범위까지 같이 본다.
- [x] 상태 전이 테스트는 성공/중복 요청/없는 ID를 모두 묶어서 보는 편이 읽기 쉽다.
- [x] `AdminStaffServiceTest`는 이미 비밀번호 암호화, `DOCTOR` 생성, `username` 중복, 수정, 비활성화 기본 시나리오가 있다.
- [x] `AdminStaffServiceTest`의 주요 누락은 `employeeNumber` 중복 차단과 비밀번호 미입력 시 기존 비밀번호 유지 시나리오다.
- [x] `AdminDepartmentServiceTest`는 목록/상세/수정/활성화/비활성화 기본 시나리오가 있다.
- [x] `AdminDepartmentServiceTest`의 주요 누락은 생성 중복 방지와 활성화 시 없는 ID 차단 시나리오다.
- [x] 두 테스트 파일 모두 기본 커버리지는 나쁘지 않지만, 생성/수정/상태 전이별로 섹션이 섞여 있어 읽는 흐름을 한 번 정리할 여지가 있다.
- [ ] PR 직전에는 사용자 요청에 맞춰 전체 테스트 실행 여부를 다시 판단한다.