# task-018

## 목적
- [x] `workflow-018`의 진료과 등록/상세/수정/상태 관리 범위를 구현과 검증 단위로 나눠 순차적으로 진행한다.
- [x] 관리자 진료과 관리의 등록, 상세/수정, 활성/비활성 흐름을 SSR 기준으로 완성한다.
- [x] 목록 화면 인라인 등록과 상세/수정 통합 페이지 구조를 명확하게 분리한다.
- [x] 상태 전이 액션을 이름 수정과 분리해 운영 흐름을 단순하게 유지한다.
- [x] 관련 컨트롤러/서비스/템플릿/테스트를 보강해 회귀를 방지한다.

## Task 18-1. 현재 진료과 구조 점검
- [x] `AdminDepartmentController`의 목록/등록 URL과 현재 계약을 점검한다.
- [x] `AdminDepartmentService`와 `Department` 엔티티의 현재 필드를 확인한다.
- [x] 상세/수정/상태 전이에 필요한 입력값과 출력값을 정리한다.
- [x] 기존 목록 화면 인라인 등록 구조를 유지해야 하는 지점을 메모한다.
- [x] 상세 페이지 구현 시 재사용 가능한 DTO/응답 구조 후보를 정리한다.

## Task 18-2. 상세/수정 통합 화면 응답 구조 정의
- [x] `GET /admin/department/detail` 렌더링에 필요한 응답 모델을 정의한다.
- [x] 진료과 기본 정보, 상태 라벨, 상태 배지용 데이터 구성을 정리한다.
- [x] 수정 폼에서 사용하는 `departmentId`, `name` 바인딩 구조를 정리한다.
- [x] 활성/비활성 버튼 노출 조건을 응답 모델에 반영할지 결정한다.
- [x] `pageTitle + model` 패턴에 맞는 템플릿 계약을 확정한다.

## Task 18-3. 상세/수정 통합 화면 구현
- [x] `GET /admin/department/detail?departmentId={id}` 컨트롤러를 구현한다.
- [x] 존재하지 않는 `departmentId` 요청에 대한 예외 처리를 연결한다.
- [x] 상세 페이지 템플릿을 추가하거나 기존 템플릿을 확장한다.
- [x] 화면에 진료과 기본 정보, 상태 배지, 이름 수정 폼을 표시한다.
- [x] 상태에 따라 활성화/비활성화 버튼이 다르게 보이도록 반영한다.

## Task 18-4. 이름 수정 기능 구현
- [x] `POST /admin/department/update`를 구현한다.
- [x] 수정 가능 항목은 `진료과명` 하나로 제한한다.
- [x] 빈 이름 또는 중복 이름 수정 요청 검증을 처리한다.
- [x] 수정 성공 후 상세 페이지로 리다이렉트한다.
- [x] 수정 성공/실패 메시지 표시 방식을 정리한다.

## Task 18-5. 상태 전이 기능 구현
- [x] `POST /admin/department/deactivate`를 구현한다.
- [x] `POST /admin/department/activate`를 구현한다.
- [x] 이미 비활성인 진료과 재비활성화 요청을 방어한다.
- [x] 이미 활성인 진료과 재활성화 요청을 방어한다.
- [x] 모든 상태 전이 성공 후 상세 페이지로 리다이렉트한다.

## Task 18-6. 목록 인라인 등록 및 테스트 정리
- [x] 목록 화면 인라인 등록 폼 액션을 `POST /admin/department/create` 기준으로 최종 맞춘다.
- [x] 별도 등록 화면 URL이 남아 있으면 제거 또는 비가시 상태로 정리한다.
- [x] `AdminDepartmentControllerTest`에 상세/수정/상태 전이 시나리오를 최소 조건으로 보강한다.
- [x] `AdminDepartmentServiceTest`에 이름 수정과 상태 전이 검증이 유지되는지 확인한다.
- [x] `admin.department` 범위 테스트를 실행해 전체 흐름을 확인한다.

## 완료 기준
- [x] 목록 화면에서 인라인 폼으로 진료과 등록이 가능하다.
- [x] `GET /admin/department/detail?departmentId={id}`에서 상세/수정 통합 화면이 렌더링된다.
- [x] 진료과명 수정이 가능하다.
- [x] 상태에 따라 활성/비활성 버튼이 다르게 표시된다.
- [x] 비활성화/활성화 처리가 가능하다.
- [x] 모든 액션 성공 후 상세 페이지로 리다이렉트된다.
- [x] 관련 SSR 컨트롤러/서비스 테스트가 통과한다.

## 추천 진행 순서
- [x] Task 18-1 현재 구조 점검
- [x] Task 18-2 상세/수정 응답 구조 정의
- [x] Task 18-3 상세/수정 통합 화면 구현
- [x] Task 18-4 이름 수정 구현
- [x] Task 18-5 상태 전이 구현
- [x] Task 18-6 인라인 등록 최종 정리 및 테스트 보강

## 메모
- [x] `workflow-018`은 진료과 등록/상세/수정/상태 관리 명세이고, `task-018`은 실제 구현 분할 문서로 사용한다.
- [x] 별도 등록 화면은 만들지 않고 목록 화면 인라인 등록 구조를 유지한다.
- [x] 상세와 수정은 같은 페이지에서 처리하고, 상태 변경은 별도 액션으로 분리한다.
- [x] 모든 성공 액션은 상세 페이지 복귀 흐름을 기준으로 구현한다.
- [x] 기존 `/admin/department/form` 계약은 제거하고, 등록은 `/admin/department/create` 하나로 통일했다.
- [x] `Department` 엔티티는 `id`, `name`, `active`를 보유하고 있으며 이름 수정은 `rename()`, 상태 전이는 `activate()`와 `deactivate()`로 분리했다.
- [x] 상세/수정 응답 모델은 `AdminDepartmentDetailResponse`로 정리하고, `departmentId`, `name`, 상태 라벨/배지, 활성/비활성 버튼 노출 조건, 액션 URL을 모두 포함하는 계약으로 확정했다.
- [x] 상세 페이지는 `department-detail.mustache`로 분리했고, 없는 진료과 상세 요청은 404 상태와 `error/404` 화면으로 처리한다.
- [x] 이름 수정은 성공 시 상세 페이지로, 검증 실패 시 다시 상세 페이지로, 대상이 없으면 목록 페이지로 리다이렉트하며 flash 메시지로 안내한다.
- [x] 상태 전이도 이름 수정과 같은 PRG 패턴을 따르며, 성공 시 상세 페이지로, 중복 상태 전이는 상세 페이지로, 대상이 없으면 목록 페이지로 리다이렉트한다.