# task-018

## 목적

- [ ] `workflow-018`의 진료과 등록/상세/수정/상태 관리 범위를 구현/검증 단위로 나눠 순차적으로 진행한다.
- [ ] 관리자 진료과 관리의 등록, 상세/수정, 활성/비활성 흐름을 SSR 기준으로 완성한다.
- [ ] 목록 화면 인라인 등록과 상세/수정 통합 페이지 구조를 명확히 분리한다.
- [ ] 상태 전이 액션을 수정 폼과 분리해 운영 흐름을 단순하게 유지한다.
- [ ] 관련 컨트롤러/서비스/템플릿 테스트를 보강해 회귀를 방지한다.

## Task 18-1. 현재 진료과 구조 점검

- [x] `AdminDepartmentController`의 목록/등록 URL과 현재 계약을 점검한다.
- [x] `AdminDepartmentService`와 `Department` 엔티티의 현재 필드를 확인한다.
- [x] 상세/수정/상태 전이에 필요한 입력값과 출력값을 정리한다.
- [x] 기존 목록 화면 인라인 등록 구조를 유지해야 하는 지점을 메모한다.
- [x] 상세 페이지 구현 시 재사용 가능한 DTO/응답 구조 후보를 식별한다.

## Task 18-2. 상세/수정 통합 화면 응답 구조 정의

- [ ] `GET /admin/department/detail` 렌더링에 필요한 응답 모델을 정의한다.
- [ ] 진료과 기본 정보, 상태 라벨, 상태 배지용 데이터 구성을 정리한다.
- [ ] 수정 폼에서 사용할 `departmentId`, `name` 바인딩 구조를 정리한다.
- [ ] 활성/비활성 버튼 노출 조건을 응답 모델에 반영할지 결정한다.
- [ ] `pageTitle + model` 패턴에 맞는 템플릿 계약을 확정한다.

## Task 18-3. 상세/수정 통합 화면 구현

- [ ] `GET /admin/department/detail?departmentId={id}` 컨트롤러를 구현한다.
- [ ] 존재하지 않는 `departmentId` 요청에 대한 예외 처리를 연결한다.
- [ ] 상세 페이지 템플릿을 추가하거나 기존 템플릿을 확장한다.
- [ ] 화면에 진료과 기본 정보, 상태 배지, 이름 수정 폼을 표시한다.
- [ ] 상태에 따라 활성화/비활성화 버튼이 다르게 보이도록 반영한다.

## Task 18-4. 이름 수정 기능 구현

- [ ] `POST /admin/department/update`를 구현한다.
- [ ] 수정 가능 항목을 `진료과명` 하나로 제한한다.
- [ ] 빈 이름 또는 중복 이름 수정 요청 검증을 처리한다.
- [ ] 수정 성공 후 상세 페이지로 리다이렉트한다.
- [ ] 수정 성공/실패 메시지 표시 방식을 정리한다.

## Task 18-5. 상태 전이 기능 구현

- [ ] `POST /admin/department/deactivate`를 구현한다.
- [ ] `POST /admin/department/activate`를 구현한다.
- [ ] 이미 비활성인 진료과 재비활성화 요청을 방어한다.
- [ ] 이미 활성인 진료과 재활성화 요청을 방어한다.
- [ ] 모든 상태 전이 성공 후 상세 페이지로 리다이렉트한다.

## Task 18-6. 목록 인라인 등록 및 테스트 정리

- [ ] 목록 화면 인라인 등록 폼 액션을 `POST /admin/department/create` 기준으로 최종 점검한다.
- [ ] 별도 등록 화면 URL이 남아 있으면 제거 또는 비사용 상태로 정리한다.
- [ ] `AdminDepartmentControllerTest`에 상세/수정/상태 전이 시나리오를 추가한다.
- [ ] `AdminDepartmentServiceTest`에 이름 수정과 상태 전이 검증을 추가한다.
- [ ] `admin.department` 범위 테스트를 실행해 전체 흐름을 확인한다.

## 완료 기준

- [ ] 목록 화면에서 인라인 폼으로 진료과 등록이 가능하다.
- [ ] `GET /admin/department/detail?departmentId={id}`에서 상세/수정 통합 화면이 렌더링된다.
- [ ] 진료과명 수정이 가능하다.
- [ ] 상태에 따라 활성/비활성 버튼이 다르게 표시된다.
- [ ] 비활성화/활성화 처리가 가능하다.
- [ ] 모든 액션 성공 후 상세 페이지로 리다이렉트된다.
- [ ] 관련 SSR 컨트롤러/서비스 테스트가 통과한다.

## 추천 진행 순서

- [x] Task 18-1 현재 구조 점검
- [ ] Task 18-2 상세/수정 응답 구조 정의
- [ ] Task 18-3 상세/수정 통합 화면 구현
- [ ] Task 18-4 이름 수정 구현
- [ ] Task 18-5 상태 전이 구현
- [ ] Task 18-6 인라인 등록 최종 점검 및 테스트 보강

## 메모

- [x] `workflow-018`은 진료과 등록/상세/수정/상태 관리 명세이고, `task-018`은 실제 구현 분할 문서로 사용한다.
- [x] 별도 등록 화면은 만들지 않고 목록 화면 인라인 등록 구조를 유지한다.
- [x] 상세와 수정은 같은 페이지에서 처리하고, 상태 변경은 별도 액션으로 분리한다.
- [x] 모든 성공 액션 후 상세 페이지 복귀 흐름을 기준으로 구현한다.
- [x] 현재 목록 인라인 등록은 컨트롤러와 템플릿 모두 `/admin/department/form`을 사용 중이라 `workflow-018`의 `/admin/department/create`로 맞추는 후속 정리가 필요하다.
- [x] `Department` 엔티티는 현재 `id`, `name`, `active`만 보유하고 `update(name, active)`만 제공하므로 이름 수정과 상태 전이를 분리하는 메서드 정리가 필요하다.
- [x] 재사용 가능한 현재 응답 구조는 `AdminDepartmentDto`와 `AdminDepartmentListResponse`까지이며, 상세/수정 화면에는 기본 정보와 상태 액션 노출 조건을 담는 별도 응답 모델이 필요하다.
